# Banco de Dados — Nexus Business Manager

## Estrutura

- `schema.sql` — Criação do banco e tabelas iniciais
- `seed.sql` — Dados iniciais para desenvolvimento

## Como Executar

```bash
mysql -u root -p < database/schema.sql
mysql -u root -p < database/seed.sql
```
